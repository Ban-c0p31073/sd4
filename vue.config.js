module.exports = {
  outputDir: 'docs',
  filenameHashing: false,
  //githubにプッシュするときだけ、下記の3行をコメントアウト
  // publicPath: process.env.NODE_ENV === 'production'
  // // ? '/project_sd4/'
  // : '/'
}